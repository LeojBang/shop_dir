services:

  db:
    image: postgres:17

    env_file:
      - .env

    volumes:
      - postgres_data:/var/lib/postgresql/data

    restart: always

  bot:
    build: .

    env_file:
      - .env

    depends_on:
      - db

    restart: always

volumes:
  postgres_data: